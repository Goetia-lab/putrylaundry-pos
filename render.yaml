services:
  - type: web
    name: putrylaundry-pos
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: GAUTH_JSON
        sync: false
      - key: SECRET_KEY
        generateValue: true